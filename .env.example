# The Whisper gate and the spoken replies (OpenAI TTS).
OPENAI_API_KEY=
# A staff JWT you already hold. Leave empty and use `--login EMAIL` instead ...
GLASSES_JWT=
# ... which mints the wearer's session on this dev project (service role, magic
# link) and clocks them in. SUPABASE_SERVICE_KEY is the older name of the same key.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
